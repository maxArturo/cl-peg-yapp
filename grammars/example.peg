
# Here is a grammar for basic math
AddExpr  <- ('+'/'-') Factor
Expr     <- Factor AddExpr*
Factor   <- Primary MulExpr*
MulExpr  <- ('*'/'/') Primary
Primary  <- '(' Expr ')'
          / Number
          / Variable
          / '-' Primary

Number   <- [0-9]+
Variable <- [A-Za-z]+

