# Here is a grammar for basic math

Expr     <- Factor AddExpr*
Factor   <- Primary MulExpr*
Primary  <- '(' Expr ')'
          / Number
          / Variable
          / '-' Primary
AddExpr  <- ('+'/'-') Factor
MulExpr  <- ('*'/'/') Primary
Number   <- [0-9]+
Variable <- [A-Za-z]+

